<UserControl x:Class="MiniETL.UI.FilePathInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" x:Name="MainControl"
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		<TextBox Grid.Column="0" Text="{Binding FilePath, ElementName=MainControl}" VerticalContentAlignment="Center" />
		<Button Grid.Column="1" Margin="3,0,0,0" Click="ButtonBrowseClick">...</Button>
	</Grid>
</UserControl>
